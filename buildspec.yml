version: 0.2

phases:
  pre_build:
    commands:
      - echo $CODEBUILD_SOURCE_VERSION
      - echo $build_version
      - git branch
      - git --version
      - git --help
  build:
    commands:
      - echo ${BRANCH}
